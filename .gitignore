# dbt
dbt/target/
dbt/logs/
dbt/.user.yml
# Note: dbt_packages/ is vendored into git intentionally — native dbt on
# Snowflake requires packages to be installed at CREATE DBT PROJECT time.

# DCM / dbt build output (temp dirs created by deploy.py)
dcm/.build/
dbt/.build/

# Local env / secrets (never commit)
.env
.env.*
!.env.example
*.pem
*.key
*.p8
*.p12
credentials.json
**/secrets/

# Snowflake CLI
.snow/
.snowflake/
connections.toml

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# Exercise 2 solution artifacts (attendees create these during the HOL)
dbt/models/marts/mart_weekly_battery_by_region.sql
dbt/models/marts/mart_top10_problematic_devices.sql
dbt/models/marts/mart_device_age_cohort_analysis.sql

# IDE
.vscode/
.idea/
*.swp
*.swo
